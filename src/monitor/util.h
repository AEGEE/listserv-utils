#include <liblistserv.h>
int keyword_doesnot_contain (struct listserv*, const char*,
			     const char*, const char*);
int keyword_does_contain (struct listserv*, const char*,
			  const char*, const char*);
int keyword_try_update (struct listserv*, const char* const,
		    const char const *, const char const *);
